<div class="container">
	<% Schedule.where(tutor_id: params[:tutor_id], applied: true).each do |s| %>
		튜터 ID: <%=s.tutor_id%><br>
		수업 시작 시간: <%=s.start_time%><br>
		수업 진행 시간(분): <%=s.time_option%><br>
		<hr>
	<%end%>
</div>